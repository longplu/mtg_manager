<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="/styles.css">
    <link href="https://unpkg.com/@saeris/typeface-beleren-bold@latest/index.css" rel="stylesheet" type="text/css" />

    <title>MTG Wish List</title>
  </head>
  <body>
    <header><h2 class="indent2rem">MTG Wish List <a href="/search" id="addCards">+add cards</a></h2></header>

        
    
    <ul>
      <% cards.forEach(card => { %>
         <li class="marginListItem">
          <form action='/<%= card.id %>?_method=DELETE' method='POST'>
           <a href='/<%= card._id %>/edit'><%= card.qty%>x</a>

            <a href="/<%= card._id %>">
             <%=card.name %>
            </a>

            <%= card.mana_cost %>
            $<%= (card.prices_usd*card.qty).toFixed(2) %>

            
              <input type="submit" value="X">
            </form>
        </li>
      <% }) %>
    </ul>

    

    <% let Total = 0; %>

    <h3 class="indent2rem"><% cards.forEach(card => { %>
        <% Total += (card.prices_usd*card.qty) %>
      <% }) %>
      TOTAL: $<%= Total.toFixed(2) %>
  </h3>

  <div class="fillerDiv"></div>

  <footer>&copy; 2021 Landlocked Muggles inc.</footer>
  </body>
</html>